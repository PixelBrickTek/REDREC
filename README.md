# REDREC Website

Static REDREC product website for GitHub Pages.

## Publish

Upload the repository contents and enable GitHub Pages from the repository root. No build step is required.

The site uses relative paths so navigation, styles, scripts, and images work from a GitHub Pages project subpath.
